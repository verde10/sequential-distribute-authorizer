[project]
name = "sequential-distribute-authorizer"
authors = ["project-author"]
description = "A smart contract for sequentially distributing and managing authorized tokenized assets"
telemetry = false
requirements = []
[contracts.sequential_distributor]
path = "contracts/sequential_distributor.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
